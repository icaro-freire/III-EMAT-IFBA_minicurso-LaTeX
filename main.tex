% arara: lualatex 
% arara: lualatex if found('log', 'undefined references')

%% arara: clean: { extensions: [aux, log, out, pdf, listing, toc] }

% classe do documento =====================================================
\documentclass[12pt]{ematIFBA}

\usepackage{pacotes} %------------------------------> metapacote para LaTeX

% início do documento =====================================================
\begin{document}
%--------------------------------------------------------------------------
  \setlength\emergencystretch{\hsize}
%
  \capa
%
  \include{tex/info.tex}
  \include{tex/intro.tex}
%
  \tableofcontents
%
  \include{tex/01_cap_o-que-e-LaTeX.tex}
%--------------------------------------------------------------------------
\end{document}
% fim do documento ========================================================
